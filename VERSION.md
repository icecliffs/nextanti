# 0.3.4

- 新增网站白名单功能：popup 面板一键将当前网站加入/移出白名单，白名单内网站完全不检测不弹窗
- 新增通知节流机制：同一标签页 30 秒内最多弹一条通知，避免弹窗轰炸
- 白名单数据通过 chrome.storage.local 持久化存储，重启浏览器后仍然生效
- 修复总防护开关关闭后 UA 旋转仍然运行的问题
- 修复总防护开关关闭后已有 session UA 规则未清理的问题
- 修复 bootstrap() 竞态条件：极端情况下 DOMContentLoaded 在 loadConfig 等待期间触发导致 init() 不执行
- 修复 inject.js cookie/localStorage/sessionStorage/eval/Function hook 不检查总开关的问题
- 修复 MAIN_DOMAIN_ORIGINS 内存泄漏：标签页关闭时未清理对应主域映射

# 0.3.3-fix

- 吃了口松饼

# 0.3.3

- 修复“总防护开关”不生效的问题：关闭后不再继续执行拦截逻辑，开启后恢复生效
- 修复“指纹混淆 / WebRTC 防泄漏”开关不生效的问题：对应能力可独立开关
- 新增开关状态的实时同步机制：`popup -> storage -> content -> inject` 全链路即时更新
- 背景动态规则与主开关联动：主开关关闭时清空动态拦截规则，开启时自动恢复
- 插件启动时按已保存配置恢复，不再默认强制全开
- 新增按站点随机 UA 机制：每次进入站点自动切换 UA，并对该站点后续请求统一生效
- 删除无限 debugger，并添加更多 User-Agent 头，添加 X-Forwarded-For 对抗某罐
- 切换主域时自动清理上一主域，访问新的站点自动删除旧站点的 Cookie 信息
- 主题改成亮色，太暗对眼睛不好

# 0.3.2

- 添加随机 UA 头，随机混淆头，使其看起来更像个人，防止被爬虫识别
- 修复插件面板显示数量错误的 bug

# 0.3.1

- 修复若干小问题，准备发版
- 添加新的 JSONP 地址

# <= 0.3.1

- 团队内部更新。